# This is a basic workflow to help you get started with Actions

name: Pull assets

# Controls when the workflow will run
on:
  # Triggers the workflow on push or pull request events but only for the main branch
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    steps:
      - name: Fetch JSON
      - run: curl --location --request GET 'https://platform.segmentapis.com/v1beta/workspaces/segment-sa/tracking-plans/rs_1tj2LiXXBrOytuy4MiE4ZJjFm8b' --header 'Authorization: Bearer efzxVOsA692Zy6wTPbJCnMRfVo50cw0TRfB5F5sDC5Q.lx0AlVaZare2fT71WlYLsW49kS3p9TyH8micIxRSjMk'
    
      - name: Publish json
      - uses: EndBug/add-and-commit@v7
        with:
          message: Show GitHub Actions logo
          committer_name: GitHub Actions
          committer_email: actions@github.com
